# 提升日志

## 2026-09-01：开源发布前安全与合规整改

### 已完成

- 将富途用户 ID、交易账户 ID、交易解锁口令 MD5、OpenD 地址和 RSA 私钥路径迁移到环境变量。
- 将 MySQL 生产及测试连接参数迁移到 Spring 配置或环境变量。
- 删除仓库内不可直接再分发的富途 API 源码和 JAR，改为由使用者自行安装官方 SDK。
- 删除构建产物、Notebook checkpoint 和未经确认再分发授权的行情 CSV。
- 清空策略 Notebook 的执行输出、执行计数和本机路径。
- 将启动脚本中的个人绝对路径替换为仓库相对路径。
- 扩充 `.gitignore`，阻止密钥、数据库、行情数据、Notebook 临时文件和构建产物再次提交。
- 添加 Apache License 2.0，并更新 README 中的环境配置、SDK 安装和数据许可说明。

### 验证

- 修改后的 Maven POM 已通过 `xmllint --noout` XML 结构检查。
- 四个 Notebook 已通过 Jupyter `nbconvert --clear-output` 清理。
- 已安装 JDK 8；首次聚合编译发现富途 SDK 5.5.1707 缺少项目使用的 `getExchType()` API，因此恢复为所需的 6.2.2707 API 版本。
- 使用本地安装的富途 SDK 6.2.2707 执行 `./mvnw -DskipTests compile`，全部 10 个 Maven 模块编译成功。
- 执行 `./mvnw test` 时，既有 `FeatureGenServiceTest` 因缺少 `com.mysql.jdbc.Driver` 且依赖真实 MySQL 而失败；失败发生在本轮改动完成测试编译之后，其他后续模块被 Maven 跳过。

### 发布前阻断项

- 确认带有 `ymm56.com`、`amh-group.com` 和 `All Rights Reserved` 声明的源码权属。
- 轮换历史中暴露过的交易解锁口令，并检查相关交易账户安全状态。
- 主分支已重建为单一根提交并强制推送；GitHub 仍保留 3 个只读 `refs/pull/*`，需联系 GitHub Support 执行敏感数据清除，或在公开前删除并重建仓库。
- 修复或隔离依赖真实 MySQL 且缺少 JDBC 驱动的 `FeatureGenServiceTest`，使自动化测试可离线运行。
